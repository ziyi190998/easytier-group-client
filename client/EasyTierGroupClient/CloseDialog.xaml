<Window x:Class="EasyTierGroupClient.CloseDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="关闭客户端" Width="360" SizeToContent="Height"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        WindowStyle="SingleBorderWindow" ShowInTaskbar="False"
        Background="#1B1D24" Foreground="#E6E8EF">
    <StackPanel Margin="22,18,22,16">
        <TextBlock Text="要退出客户端，还是隐藏到托盘？" FontSize="14" FontWeight="SemiBold" Margin="0,0,0,4"/>
        <TextBlock Text="隐藏后保持连接，可随时从托盘图标右键退出。" FontSize="11" Foreground="#9AA0AC"
                   Margin="0,0,0,14" TextWrapping="Wrap"/>

        <RadioButton x:Name="RbHide" GroupName="close" IsChecked="True" Margin="0,0,0,8">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="隐藏进托盘" FontSize="13"/>
                <TextBlock Text="（保持连接）" FontSize="11" Foreground="#9AA0AC" VerticalAlignment="Center"/>
            </StackPanel>
        </RadioButton>
        <RadioButton x:Name="RbExit" GroupName="close" Margin="0,0,0,12">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="退出程序" FontSize="13"/>
                <TextBlock Text="（断开与服务器的连接）" FontSize="11" Foreground="#9AA0AC" VerticalAlignment="Center"/>
            </StackPanel>
        </RadioButton>

        <CheckBox x:Name="ChkRemember" Content="记住设置，下次不再提示" FontSize="12" Margin="0,0,0,16"/>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="取 消" Width="84" Height="30" Margin="0,0,10,0" IsCancel="True"/>
            <Button Content="确 定" Width="84" Height="30" IsDefault="True" Click="OkButton_Click"/>
        </StackPanel>
    </StackPanel>
</Window>
